# Phase 3 — অংশ ২: Add Customer → Dedicated Route

## ✅ যা হলো

- **নতুন পেজ**: `pages/worker/AddCustomer.jsx` — পুরো wizard (ছবি/GPS/ফর্ম/email OTP/WhatsApp) এখন এখানে, `/worker/customers/new` route-এ। আগে এটা `CustomerList.jsx`-এর ভেতরে একটা modal ছিল।
- **`CustomerList.jsx` আরও হালকা হলো** — wizard-এর সব state/handler সরে যাওয়ায় ৩৭৩ → বর্তমানে ছোট। "নতুন" বাটন এখন সরাসরি নতুন route-এ navigate করে।
- **Existing কাস্টমারের WhatsApp-resend আলাদা হয়ে গেল** — এটা wizard-এর অংশ ছিল না সত্যিই (শুধু আগে থেকে-থাকা একটা কাস্টমারের লিংক আবার পাঠানো), তাই এখন নিজের ছোট `BottomSheet`-এ, `CustomerList.jsx`-এই থেকে গেছে।
- **Cache sync**: নতুন পেজ থেকে কাস্টমার তৈরি হলে `useInvalidateCustomers()` (Phase 1-এ বানানো হুক) দিয়ে সংশ্লিষ্ট route-এর cache invalidate হয় — "কাস্টমার তালিকা"-য় ফিরলে React Query নিজে থেকেই fresh ডেটা আনবে।
- **ছোট, ইচ্ছাকৃত উন্নতি**: নতুন পেজে "রুট" ড্রপডাউন এখন ডিফল্টভাবে SR-এর বর্তমান সিলেক্ট করা রুট দেখায় (আগে খালি থাকত) — যৌক্তিক কারণ SR প্রায় সবসময় নিজের চলমান রুটেই কাস্টমার যোগ করে।

## 🛠️ একটা গুরুত্বপূর্ণ টুলিং উন্নতি — এবং যে বাগ এটা ধরল

CustomerList.jsx থেকে wizard সরানোর সময় আমার সিনট্যাক্স-checker (TypeScript দিয়ে বানানো) একটা জিনিস miss করে যাচ্ছিল: **এটা শুধু সিনট্যাক্স ধরে, "undefined variable ব্যবহার" ধরে না** (যেমন কোনো state মুছে ফেলার পর সেই state ব্যবহার করা কোনো function ভুলে রয়ে গেলে)। ঠিক এটাই হয়েছিল — `getGPS`/`handlePhoto` ফাংশন দুটো রয়ে গিয়েছিল যদিও তাদের ব্যবহৃত `gps`/`photo`/`setGpsLoading` state মুছে ফেলা হয়েছিল। Syntax ঠিক ছিল, কিন্তু চালালে "X is not defined" রানটাইম এরর দিত।

এটা ধরার পর আরেকটা, শক্তিশালী checker বানালাম (TypeScript-এর সেমান্টিক analysis দিয়ে, শুধু "cannot find name" ক্লাসের এরর টার্গেট করে) — সেটা দিয়ে **এই পুরো প্রজেক্টে আমার তৈরি/সম্পাদিত সব ফাইল** (Phase 1, 2, 3 — ১৪টা ফাইল) আবার চেক করলাম রেট্রোঅ্যাক্টিভলি, যাতে আগের ধাপেও একই ধরনের কিছু চোখ এড়িয়ে না যায়। সব ক্লিন এসেছে — এই একটাই instance ছিল, এখন ঠিক করা।

## ⚠️ একটা অসম্পর্কিত, পুরনো বাগ পেলাম (ঠিক করিনি, শুধু জানাচ্ছি)

নতুন checker চালানোর সময় `backend/src/controllers/customer.controller.js`-এর **সম্পূর্ণ ভিন্ন একটা ফাংশনে** (`collectCredit`, বাকি আদায় — আমি এই ফাংশন কখনো ছুঁইনি) একটা real bug চোখে পড়ল: `amount` ভ্যারিয়েবল `try` ব্লকের ভেতরে destructure করা (`const { amount, ... } = req.body`), কিন্তু `catch` ব্লকে (idempotency-key duplicate-request handling-এ) সেটা আবার ব্যবহার করা হচ্ছে — এই দুটো আলাদা block scope, তাই `catch`-এ `amount` আসলে undefined। এটা শুধু একটা নির্দিষ্ট রেস-কন্ডিশনে (network retry থেকে duplicate collection request) ঘটবে, তখন SR-কে বন্ধুত্বপূর্ণ "সফল হয়েছিল" মেসেজের বদলে সার্ভার এরর দেখাবে।

**এটা ইচ্ছাকৃতভাবে ঠিক করিনি** — এটা payment/collection-সম্পর্কিত কোড, আমার scope-এর বাইরে, আর না চাইতে টাচ করাটা ঠিক হবে না। জানিয়ে রাখলাম যাতে আপনি প্রয়োজনে দেখে নিতে পারেন (ফিক্স সহজ — `amount`-কে `try` ব্লকের বাইরে declare করা)।

## 🔧 আপনার করণীয়

কিছু না নতুন — dependency অপরিবর্তিত। টেস্ট করার সময়: (১) "নতুন" বাটনে পেজ বদলে যাচ্ছে কিনা (মডাল না), (২) কাস্টমার তৈরির পর "কাস্টমার তালিকা"-য় ফিরলে নতুনটা দেখা যাচ্ছে কিনা, (৩) existing কাস্টমারের WhatsApp বাটনে চাপলে ছোট bottom-sheet খুলছে কিনা।
