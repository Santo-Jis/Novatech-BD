# Phase 2 — Route Select পেজ রিডিজাইন (UX)

Phase 1 ছিল আর্কিটেকচার (ভিজ্যুয়াল অপরিবর্তিত)। এটা প্রথম দফা যেখানে SR-রা আসলে পর্দায় নতুন কিছু দেখবেন।

## ✅ নতুন কী দেখা যাবে

- **"আজকের অগ্রগতি" সারসংক্ষেপ** — হেডারের নিচে, নিজের রুট(গুলো) মিলিয়ে মোট কাস্টমার/আজকের ভিজিট/বকেয়ার একনজর progress bar। শুধু নিজের রুট থাকলেই দেখা যাবে (`mine.length > 0`), নাহলে জায়গা নেয় না।
- **প্রতিটা route card-এ progress bar** — এখন শুধু টেক্সট (`X কাস্টমার · Y ভিজিট`) না, visually কতটুকু বাকি সেটাও বোঝা যায়।
- **"আবেদন" প্যানেল ও "নতুন রুট" ফর্ম** — দুটোই এখন Phase 1-এ বানানো shared `BottomSheet` কম্পোনেন্টে। আগে দুই জায়গায় প্রায় হুবহু bottom-sheet কোড হাতে লেখা ছিল (drag handle, backdrop, body-scroll-lock — সব ডুপ্লিকেট)।

## 🎨 ডিজাইন সিদ্ধান্ত — কী বদলায়নি এবং কেন

- **নতুন কোনো রঙ/ফন্ট আনা হয়নি।** বিদ্যমান navy `primary` আর Phase 0-এর `success`/`info` টোকেন ব্যবহার হয়েছে। এই অ্যাপে ৪০+ স্ক্রিন এখনো অপরিবর্তিত — এখানে নতুন ব্র্যান্ড কালার ঢোকালে বাকি অ্যাপের সাথে অসামঞ্জস্য তৈরি হতো।
- সারসংক্ষেপ কার্ডে ইচ্ছাকৃতভাবে **gradient wash নেই** — সাদা কার্ড, শুধু functional progress bar। এটা মার্কেটিং হিরো না, দৈনন্দিন কাজের সংখ্যা।
- কোনো card-এ নতুন entrance/hover animation যোগ করা হয়নি (শুধু বিদ্যমান `active:scale-95` ট্যাপ ফিডব্যাক) — বটম-শিটের slide-up-টাই একমাত্র নতুন motion, এবং সেটা ব্যবহারকারীর action-এর সরাসরি প্রতিক্রিয়া (খোলা/বন্ধ করা), আলাদা করে সাজানো effect না।

## ⚠️ একটা ছোট, ইচ্ছাকৃত আচরণ পরিবর্তন

"আবেদন" বাটন আগে **toggle** করত (আবার চাপলে বন্ধ হতো)। এখন এটা bottom sheet-এর নিজস্ব close (X বাটন/backdrop ট্যাপ) দিয়ে বন্ধ হয় — বাটনে আবার চাপলে কিছু হয় না (idempotent open)। এটা bottom-sheet প্যাটার্নের স্বাভাবিক convention (backdrop-এর নিচে ট্রিগার বাটনটা আর দেখাই যায় না), তাই টগল না রাখাই স্বাভাবিক।

## 🔧 আপনার করণীয়

কিছু না — কোনো নতুন dependency লাগেনি। রুট সিলেক্ট পেজ খুলে (১) সারসংক্ষেপ কার্ড ঠিকমতো দেখাচ্ছে কিনা, (২) route card-এর progress bar, (৩) "আবেদন"/"নতুন রুট" বাটন চেপে bottom sheet ঠিকমতো খুলছে-বন্ধ হচ্ছে কিনা দেখে নিন।

---

**পরের ধাপ: Phase 3** — Customer List/Map পেজের UX রিডিজাইন (map↔list split view, marker clustering, list virtualization)।
